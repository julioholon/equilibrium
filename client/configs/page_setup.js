/* global DocHead */
var title = "Equilibrium";
DocHead.setTitle(title);

var metaInfo = {name: "description", content: "Equilibrium Social Network"};
DocHead.addMeta(metaInfo);

var linkInfo = {rel: "stylesheet", href: "https://fonts.googleapis.com/icon?family=Material+Icons"};

DocHead.addLink(linkInfo);

var linkInfo2 = {rel: "stylesheet", href: "https://fonts.googleapis.com/css?family=Roboto:400,300,500"};

DocHead.addLink(linkInfo2);
